window.addEventListener('scroll', function() {
    const imgContainer = document.querySelector('.img-container');
    // For fixed positioning, CSS should handle this; JavaScript here is for additional logic if needed
    // Example: Change opacity or add other styles based on scroll position
});